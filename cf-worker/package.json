{
  "name": "yugioh-image-proxy",
  "version": "1.1.0",
  "type": "module",
  "description": "Cloudflare Worker CORS proxy for YGOPRODeck images and search API",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "login": "wrangler login"
  },
  "devDependencies": {
    "wrangler": "^3.114.0"
  }
}
