{
  "name": "yugioh-image-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker CORS proxy for YGOPRODeck images",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "login": "wrangler login"
  },
  "devDependencies": {
    "wrangler": "^3.114.0"
  }
}
